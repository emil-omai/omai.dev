baseURL = '/'

title = "Omai"

[params]
  description = "Omai's hemsida"
  author = "Omai"
  canonicalURL = "https://omai.dev"  # Canonical URL for Open Graph and absolute links
  turnstileSiteKey = "0x4AAAAAACNQSMXZq9WoK5m_"
  cloudflareAnalyticsToken = "41e94da49f91427db3c2b59aa75e945e"  # Add your Cloudflare Web Analytics token here

disableKinds = ['taxonomy', 'term', 'taxonomyTerm']

[permalinks]
  om-ai = '/om-ai/:slug/'
  case = '/case/:slug/'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

ignoreLogs = ['warning-goldmark-raw-html']

# Minification settings
[minify]
  disableHTML = false
  disableCSS = false
  disableJS = false
  disableJSON = false
  disableSVG = false
  disableXML = false
  minifyOutput = true
